# Base URL of the Capybara FastAPI backend.
# For local development the backend typically runs on port 8000.
# When deploying to Vercel, set this to the backend's public HTTPS URL.
NEXT_PUBLIC_API_BASE=http://localhost:8000
